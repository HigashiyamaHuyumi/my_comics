<h1>タグ一覧</h1>

<ul>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, edit_tag_path(tag) %> |
      <%= link_to '削除', tag, method: :delete, data: { confirm: '本当に削除しますか？' } %>
    </li>
  <% end %>
</ul>

<%= link_to '新しいタグを追加', new_tag_path %>