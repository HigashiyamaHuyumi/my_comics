<!-- エラーチェック -->
<% if comic.errors.any? %>
  <%= comic.errors.count %> errors prohibited this comics from being saved:
  <ul>
    <% comic.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>